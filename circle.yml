machine:
  python:
    version: 3.5.0

dependencies:
  pre:
    - pip install pytest
    - pip install git+https://github.com/fangohr/micromagnetics.git
    - pip install scipy

test:
  override:
    - make test
